spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:glp_erp_homolog}
    username: ${DB_USER:glprevenda_user}
    password: ${DB_PASSWORD:glprevenda_pass}
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

server:
  port: 8080

logging:
  level:
    br.com.glprevenda: INFO
